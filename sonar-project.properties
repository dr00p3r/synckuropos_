# =====================================================
# SONARQUBE PROJECT CONFIGURATION
# =====================================================

# Identificación del proyecto
sonar.projectKey=synckuropos
sonar.projectName=SynckuroPOS
sonar.projectVersion=1.0

# Ruta de fuentes (TypeScript)
sonar.sources=synckuropos_app/src,synckuropos_server

# Excluir archivos de test y node_modules
sonar.exclusions=**/node_modules/**,**/*.test.ts,**/*.test.tsx,**/*.spec.ts

# Configuración TypeScript
sonar.typescript.lcov.reportPaths=coverage/lcov.info

# URL de SonarQube (modificar si no es local)
sonar.host.url=http://localhost:9000

# Token de autenticación
sonar.login=<TU_TOKEN_PERSONAL>

# Codificación
sonar.sourceEncoding=UTF-8
